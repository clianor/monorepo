{
  "name": "design-system",
  "version": "0.0.1",
  "scripts": {
    "lerna": "lerna",
    "build": "lerna run build --stream",
    "clean": "lerna run clean --parallel"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.2.4"
  },
  "private": true,
  "workspaces": ["packages/*"]
}
